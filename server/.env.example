# Server Configuration
PORT=3001

# CORS Configuration (comma-separated, use * for all origins)
CORS_ORIGINS=*

# MySQL Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=reaction_rush