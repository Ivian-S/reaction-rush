{
  "name": "reaction-rush",
  "version": "1.0.0",
  "private": true,
  "description": "Reaction Rush - A reaction time test game",
  "scripts": {
    "dev:frontend": "pnpm --filter frontend dev",
    "dev:server": "pnpm --filter server dev",
    "build:frontend": "pnpm --filter frontend build",
    "build:server": "pnpm --filter server build",
    "build": "pnpm build:frontend && pnpm build:server",
    "start": "pnpm --filter server start",
    "start:server-only": "pnpm --filter server build && pnpm --filter server start",
    "typecheck": "pnpm --filter frontend typecheck && pnpm --filter server typecheck"
  }
}